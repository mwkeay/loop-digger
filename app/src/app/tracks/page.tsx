import "@/styles/tracks.css";
import { FC } from "react";

const Page: FC = () => {
    return (
        <div className="tracks-page">
            <h1>You need to pick a track...</h1>
        </div>
    );
};

export default Page;
